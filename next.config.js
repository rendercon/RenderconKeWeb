/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['sessionize.com', 'vo3gv2h0n9olcv4f.public.blob.vercel-storage.com'],
      },
}

module.exports = nextConfig
